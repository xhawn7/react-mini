import useFetch from "src/hooks/useFetch"
const Detail = ({dId}) =>{
  // let detailEndpoint = `https://www.thecocktaildb.com/api/json/v1/1/lookup.php?i=${dId}`
  // const {data,loading} = useFetch(detailEndpoint)
  console.log(dId)
  return <>
  134</>
}

export default Detail